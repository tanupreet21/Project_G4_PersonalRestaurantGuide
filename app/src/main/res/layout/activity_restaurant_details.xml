<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <!-- Top toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/toolbar_green"
        app:title="Restaurant Details"
        app:titleTextColor="@android:color/white" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@android:color/white"
            android:padding="16dp"
            android:elevation="2dp">

            <!-- Restaurant image -->
            <ImageView
                android:id="@+id/imageRestaurant"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_gravity="center"
                android:src="@drawable/img"
                android:layout_marginBottom="12dp" />

            <!-- EDIT button (rounded green) -->
            <Button
                android:id="@+id/buttonEdit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="EDIT"
                android:layout_gravity="center"
                android:background="@drawable/button_rounded"
                android:textColor="@android:color/white"
                android:layout_marginBottom="20dp" />

            <!-- Description label -->
            <TextView
                android:text="Description"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"
                android:textSize="15sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <!-- Description box -->
            <TextView
                android:id="@+id/textDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minLines="2"
                android:padding="14dp"
                android:textSize="14sp"
                android:background="#E0E0E0"
                android:layout_marginBottom="18dp" />

            <!-- Tags label -->
            <TextView
                android:text="Tags"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"
                android:textSize="15sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <!-- Dynamic tag chips container -->
            <LinearLayout
                android:id="@+id/tagContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="18dp" />

            <!-- Rating label -->
            <TextView
                android:id="@+id/textRatingLabel"
                android:text="Rating"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"
                android:textSize="15sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <!-- Rating bar (green stars, smaller size) -->
            <RatingBar
                android:id="@+id/ratingBar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:numStars="5"
                android:stepSize="1"
                android:isIndicator="true"
                android:scaleX="0.8"
                android:scaleY="0.8"
                android:progressTint="@color/toolbar_green"
                android:secondaryProgressTint="@color/toolbar_green"
                android:indeterminateTint="@color/toolbar_green"
                android:layout_marginBottom="22dp" />

            <!-- Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#DDDDDD"
                android:layout_marginBottom="18dp" />

            <!-- Address label -->
            <TextView
                android:text="Address"
                android:textStyle="bold"
                android:layout_marginBottom="6dp"
                android:textSize="15sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <!-- Address box -->
            <TextView
                android:id="@+id/textAddress"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="14dp"
                android:textSize="14sp"
                android:background="#E0E0E0"
                android:layout_marginBottom="22dp" />

            <!-- Map buttons row -->
            <LinearLayout
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:layout_marginBottom="18dp">

                <!-- SHOW ON MAP (rounded green) -->
                <Button
                    android:id="@+id/buttonShowMap"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="SHOW ON MAP"
                    android:background="@drawable/button_rounded"
                    android:textColor="@android:color/white"
                    android:padding="10dp" />

                <Space
                    android:layout_width="10dp"
                    android:layout_height="wrap_content" />

                <!-- GET DIRECTIONS (rounded green) -->
                <Button
                    android:id="@+id/buttonDirections"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="GET DIRECTIONS"
                    android:background="@drawable/button_rounded"
                    android:textColor="@android:color/white"
                    android:padding="10dp" />
            </LinearLayout>

            <!-- SHARE button (rounded gray) -->
            <Button
                android:id="@+id/buttonShare"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="SHARE"
                android:background="@drawable/button_rounded_gray"
                android:textColor="@android:color/black" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
